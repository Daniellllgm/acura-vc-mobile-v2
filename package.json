{
  "name": "acura-app",
  "version": "1.0.0",
  "description": "Acura Webapp para Cuidadores",
  "scripts": {
    "build": "echo 'Build estatico finalizado. Certifique-se de que seus arquivos HTML estao na pasta correta.'",
    "start": "npx cap open ios",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/android": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
